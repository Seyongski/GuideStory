# ARCHITECTURE.md — 시스템 아키텍처 & 설계 결정(ADR)

> 이 프로젝트는 "구현"이 아니라 **"설계 결정의 근거·트레이드오프·한계 증명"** 이 산출물이다.
> 따라서 이 문서의 핵심은 §4 **ADR(아키텍처 결정 기록)** 와 §5 **증명 과제 대시보드**다.
> 코드를 바꾸기 전 관련 ADR을 읽고, 결정을 바꾸면 ADR을 갱신한다.

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|------|------|------|
| IDE / 툴셋 | Visual Studio 2022 이상 (툴셋 `v143`) | `GuideStory.sln`. 상위 VS에서도 MSBuild가 v143을 설치된 툴셋으로 해석해 빌드된다(VS 18에서 확인) |
| 언어 표준 | **C++20** | ⚠️ vcxproj에 `LanguageStandard` 미설정 — [tech-debt-tracker.md](tech-debt-tracker.md) D-002 |
| 플랫폼 | Win32 / x64 (Console) | 서버·AI는 x64 권장 |
| 클라이언트 그래픽 | SDL2 (+ SDL2_ttf, SDL2_image) | 추상화 레이어 뒤로 격리 (ADR-006). 텍스트는 `IRenderDevice::DrawText`, 이미지는 `LoadTexture`/`DrawTexture`로 은닉(구현만 SDL_ttf/SDL_image). PNG 디코딩=SDL_image |
| 네트워크 | WinSock / POSIX 소켓 (`net/Socket.h` 뒤로 격리) | 데디케이트 서버 (ADR-001). 1차는 **접속당 스레드 1개** — IOCP/epoll 비교의 기준선 |
| 서버 저장소 | SQLite 3.47.1 (앰알가메이션 동봉) | 계정 + 채팅 로그. vcpkg 없이 서버 단독 빌드 — [`ThirdParty/sqlite`](ThirdParty/sqlite/README.md) |
| 비밀번호 | PBKDF2-HMAC-SHA256 (10만 회) + 16바이트 솔트 | 외부 라이브러리 없이 자체 구현(`GuideStoryServer/src/Crypto.h`). ⚠️ 전송 구간은 평문 — D-008 |
| AI 학습/추론 | Python(학습) + libtorch(C++ 추론) | ADR-007 |
| C++ ↔ Python | ZMQ 또는 Socket | ADR-007 |
| 데이터 포맷 | JSON / CSV | 데이터 주도 (ADR-005) |
| 메모리 관리 | 스마트 포인터 + RAII | ADR-002 |

## 2. 시스템 토폴로지

```
┌────────────────────┐        패킷(입력/요청)        ┌──────────────────────────┐
│   클라이언트 (SDL2)  │ ───────────────────────────▶ │   데디케이트 서버 (WinSock) │
│  - 입력 수집         │                              │  - 지형/충돌 권위 계산      │
│  - 렌더 / 보간       │ ◀─────────────────────────── │  - FSM/스킬 판정           │
│  - 예측(선택)        │        브로드캐스트(결과)      │  - 멀티스레드 세션 관리      │
└────────────────────┘                              └────────────┬─────────────┘
                                                                  │ ZMQ/Socket
                                                                  ▼
                                                       ┌──────────────────────┐
                                                       │  AI 서브시스템         │
                                                       │  학습: Python          │
                                                       │  추론: libtorch(C++)   │
                                                       └──────────────────────┘
```

**서버 권위(Server-Authoritative)**: 플레이어가 이동/스킬을 쓰면 클라이언트가 즉시 적용하지 않고,
서버에 패킷을 보낸다. 서버가 지형·충돌을 계산한 뒤 결과를 모든 클라이언트에 브로드캐스트한다.

## 3. 계층 & 모듈 (목표 구조)

```
GuideStory/
├─ GuideStory.sln                  # 4 프로젝트 (ADR-009 + 서버)
├─ GuideStory/                     # ▶ GuideStoryEngine (정적 라이브러리)
│  ├─ GuideStory.vcxproj
│  └─ src/
│     ├─ core/        # 카메라, 월드 렌더(RenderWorld), Ui(메뉴/툴바 위젯), HostLoop(앱 공통 프레임 골격) 등 공용 (SDL 비의존)
│     ├─ math/        # Vector2D, Rect (SDL 비의존 — ADR-006)
│     ├─ platform/    # RenderDevice / Window / 입력 인터페이스 (+ SDL2 구현), FileDialog(네이티브 열기·저장 + 자산 경로)
│     ├─ world/       # TileMap·Foothold·Map(+배경 PNG·MapObject)·MapScaffold (ADR-008)
│     ├─ editor/      # MapEditor (편집 로직)
│     ├─ ecs/         # 컴포넌트 기반 게임 오브젝트 (ADR-003)
│     ├─ physics/     # PlatformerController; AABB, QuadTree (ADR-004)
│     ├─ ai/          # 몬스터 FSM, libtorch 추론 브리지 (ADR-007)
│     ├─ data/        # DataManager: JSON/CSV 로드·캐싱 (ADR-005)
│     └─ net/         # Protocol(서버와 공유) · Framing · Socket · NetClient(워커 스레드) (ADR-001)
├─ GuideStoryEditor/               # ▶ GuideStoryEditor.exe (편집·저장)
│  └─ main.cpp + EditorApp(호스트 루프) + EditorScreen(화면 인터페이스)
│     + LauncherScreen + MapEditorScreen + DataEditorScreen(플레이어/스킬/몬스터/NPC 공유 골격)
│     # 화면 흐름: 선택 화면 → 각 에디터(빈 화면 → 새로 만들기/열기 → 편집 → 저장)
├─ GuideStoryGame/                 # ▶ GuideStoryGame.exe (맵 로드·플레이)
│  └─ main.cpp + App(호스트 루프) + Screen(장면 인터페이스)
│     + LoginScreen / MainMenuScreen / GameScreen   # Ui 위젯은 엔진 core/로 이동(두 앱 공유)
│     # 장면 흐름: 로그인창(서버 인증) → 메인화면(게임시작/환경설정/로그아웃/게임종료) → 인게임(채팅)
└─ GuideStoryServer/               # ▶ GuideStoryServer.exe (계정·채팅 서버, 콘솔)
   └─ src/ Server(accept 루프 + 접속당 스레드) · Session · Accounts(동기 SQLite)
      · ChatLog(비동기 SQLite) · Crypto(SHA-256/PBKDF2)
      # SDL/vcpkg 비의존 — 엔진 lib을 참조하지 않고 net/의 소스만 함께 컴파일한다
```
> **서버는 엔진 라이브러리를 링크하지 않는다.** 공유하는 것은 `net/Protocol.h`·`Framing.cpp`
> 소스뿐이다. 그래야 서버만 배포하는 상황에서 SDL 의존성 트리를 복원할 필요가 없다
> (그리고 프로토콜은 한 소스에서 나오므로 양쪽이 어긋날 수 없다).
> 엔진 라이브러리는 `main`/호스트 루프를 갖지 않는다. 두 앱이 각자 합성 루트(`main`)에서
> `SDLWindow`/`SDLRenderDevice`를 생성해 `EditorApp`/`GameApp`에 주입한다 (ADR-009).

| 모듈 | 책임 | SDL 의존 | 관련 ADR |
|------|------|----------|----------|
| core | 메인 루프·상태 | ✗ | |
| math | Vector2D/Rect | ✗ (자체 정의) | ADR-006 |
| platform | 렌더/윈도우 추상화 | ✓ (격리됨) | ADR-006, ADR-002 |
| ecs | 컴포넌트 오브젝트 | ✗ | ADR-003 |
| physics | 충돌·공간분할 | ✗ | ADR-004 |
| ai | FSM·추론 | ✗ | ADR-007 |
| data | 데이터 로드 | ✗ | ADR-005 |
| net | 프로토콜·프레이밍·클라이언트 워커 | ✗ (winsock2.h는 `NetClient.cpp` 안에만) | ADR-001 |

> **불변 규칙**: `core/ecs/physics/ai/data/net` 같은 **비즈니스 로직은 SDL 네이티브 타입(SDL_Renderer, SDL_Rect 등)에 의존하지 않는다.** SDL은 `platform/` 뒤에만 존재한다. (ADR-006)

## 4. 아키텍처 결정 기록 (ADR)

> 각 ADR은 **결정 / 이유 / 트레이드오프 / 증명 과제(지침 지표)** 를 가진다.
> 증명 과제는 §5 대시보드에서 진행 상황을 추적한다.

### ADR-001 — 데디케이트 서버 (서버 권위 구조)
- **결정**: 클라이언트는 입력만 전송, 서버가 지형·충돌을 판정 후 브로드캐스트.
- **이유**: 치팅 방지, 단일 진실 원천, 멀티플레이어 동기화의 기반.
- **트레이드오프**: 입력 지연(RTT)↑ → 클라 예측/보간 필요. 서버 연산 부하↑.
- **증명 과제**: WinSock IOCP/Select 모델 비교, 멀티스레드 서버에서 N명이 한 맵에서 동기화되는지 검증. 패킷 구조체 설계 근거 기록.

#### 1단계 — 계정 + 채팅 (완료)
서버가 월드 상태를 갖기 전에, **"인증된 세션과 그들 사이의 메시지"** 까지를 먼저 세웠다.
이동 동기화를 먼저 만들면 "누가 누구인지" 가 없어서 결과를 누구에게 보낼지 정할 수 없다.

- **프로토콜은 헤더 하나를 공유한다** (`net/Protocol.h`를 서버·클라이언트가 같이 컴파일).
  각자 정의를 들고 있으면 한쪽만 고쳤을 때 조용히 어긋난다.
  - 첫 필드가 `Version`인 이유: 구조체 크기가 서로 달라도 그 2바이트는 읽을 수 있어야
    "버전이 안 맞다"고 **정확한 사유**를 돌려줄 수 있다.
  - `#pragma pack(1)` + `static_assert(sizeof(...))`: 패딩 사고를 런타임이 아니라 빌드에서 잡는다.
  - 옵코드는 **항상 끝에 추가**한다. 중간에 끼우면 뒤 번호가 밀려 엉뚱한 핸들러가 받는다.
- **길이 프리픽스 프레이밍**: TCP는 메시지 경계를 보장하지 않으므로 `BodySize`로 직접 자른다.
  선언된 길이를 신뢰하기 **전에** 상한을 검사한다(안 하면 위조된 크기로 서버 메모리가 터진다).
- **신원은 서버가 채운다**: 발신자 이름/UserId를 클라이언트 패킷에서 옮기지 않는다(사칭 방지).
- **스레드 모델**: 서버 = 접속당 스레드 1개, 클라이언트 = 소켓 워커 1개 + 사건 큐.
  게임 스레드는 소켓을, 워커 스레드는 화면을 서로 만지지 않는다.
- **저장소의 보증 수준을 갈랐다**: 계정은 동기 + `synchronous=FULL`("가입했는데 계정이 없다"는
  있을 수 없다), 채팅 로그는 비동기 큐 + `NORMAL`(몇 줄 유실보다 지연이 나쁘다).
- **알려진 한계**: 비밀번호 평문 전송(D-008), 모든 send가 세션 락 안(D-009).
- 다음 단계는 서버가 맵/좌표를 갖고 이동을 판정하는 것이다. 그때 옵코드를 뒤에 붙여 늘린다.
- 출처: `Unreal-MOU/MOU_Server`. 언리얼 의존과 4인 co-op 전제 기능(방/로비, 팀·사망 채널,
  친구·메신저)은 지속 월드에 맞지 않아 가져오지 않았다.

### ADR-002 — RAII + 스마트 포인터 (자원 수명 관리)
- **결정**: SDL의 C 스타일 할당/해제를 C++ 클래스 생성자/소멸자(RAII)로 캡슐화. `TextureManager`, `WindowManager`는 **Custom Deleter** 를 가진 스마트 포인터로 SDL 핸들 관리.
- **이유**: 누수·이중 해제 방지, 예외 안전성.
- **트레이드오프**: 래퍼 작성 비용, 약간의 간접화.
- **증명 과제**: `SDL_DestroyTexture`/`SDL_DestroyWindow` 등을 `unique_ptr` Custom Deleter로 감싼 과정과, 그로 인해 확보한 안정성을 코드/문서로 기록.

### ADR-003 — 컴포넌트 기반 게임 오브젝트
- **결정**: 초기에는 가벼운 상속으로 시작하되, 스킬·몬스터 시스템 추가 시점에 **컴포넌트 패턴** 도입.
- **이유**: 상속 폭발(diamond/조합 폭증) 회피, 기획 변경 유연성.
- **트레이드오프**: 초기 설계 비용, 컴포넌트 간 통신 복잡성·캐시 지역성 고려.
- **증명 과제**: 상속→컴포넌트 전환 시점과 동기를 기록. 예) "플레이어 특정 스킬의 상태이상 구현" 같은 기획 변경에 컴포넌트 구조가 어떻게 유연히 대처했는지 **장단점 비교 기술**.

### ADR-004 — QuadTree 공간 분할 (충돌 최적화)
- **결정**: 충돌 후보 탐색을 Brute-Force(이중 루프) 대신 **QuadTree** 로.
- **이유**: 오브젝트 수 증가 시 O(n²) 프레임 드랍 방지.
- **트레이드오프**: 동적 객체 갱신(재삽입) 오버헤드, 트리 구축 비용.
- **증명 과제**: 대량 몬스터·드롭 아이템 **스트레스 테스트**. 브루트포스 vs 쿼드트리의 **프레임타임(ms)·CPU 점유율**을 데이터로 비교, 도입 배경과 **한계(동적 갱신 오버헤드)** 명시.

### ADR-005 — 데이터 주도 설계 (JSON/CSV + DataManager)
- **결정**: 스킬(쿨타임/타격횟수/범위/데미지계수/이펙트 프레임), 몬스터 스펙, 아이템을 코드가 아닌 외부 JSON/CSV로 분리. 엔진 시작 시 `DataManager` 가 메모리에 캐싱.
- **이유**: 기획자가 코드 수정 없이 수치 조절 → 유지보수성 극대화.
- **트레이드오프**: 파일 I/O·문자열 파싱으로 초기 로딩 시간↑, 데이터 포맷 오류 시 예외 처리 난이도↑.
- **증명 과제**: 파싱 중 데이터 오염을 막는 **예외 처리** 코드 포함, 그 장단점 기록.

### ADR-006 — SDL 디커플링 (이식성 / 결합도)
- **결정**: 자체 `Vector2D`, `Rect` 구조체 정의. 렌더링을 엔진 자체 `RenderDevice`/`Window` **인터페이스 뒤로 은닉**하고 내부에서만 SDL2 호출.
- **이유**: 미래 SDL3 / DirectX / Custom Graphics API 이식 시, 인터페이스 구현체만 교체.
- **트레이드오프**: 초기 설계 비용↑, 우회 호출로 미세한 구조적 복잡성↑.
- **증명 과제**: 비즈니스 로직이 SDL 네이티브 타입에 의존하지 않음을 보장. "SDL2→SDL3 이식 시 내 코드가 얼마나 유연한가(결합도)"를 **직접 결합 vs 추상화 레이어**로 비교·분석.

### ADR-007 — C++ 엔진 ↔ AI 서브시스템 통합
- **결정**: 학습은 Python(ML 에코시스템), C++ 엔진과는 ZMQ 또는 일반 Socket으로 통신. 학습된 모델은 추후 **libtorch** 로 C++ 엔진 내부에 직접 로드해 추론.
- **이유**: 학습 생산성(Python)과 런타임 성능/배포(C++) 분리.
- **트레이드오프**: 프로세스 간 통신 지연·직렬화 비용, 빌드 복잡성(libtorch 링크).
- **증명 과제**: 통신 레이어 지연 측정, 오프라인 학습→온라인 추론 전환 과정 기록.
- **2단계 통합**: ①원격 추론(소켓+JSON, 모델 교체가 C++ 재빌드 없이 가능) → ②libtorch 인프로세스(TorchScript). **둘 다 같은 인터페이스 뒤에 두어야** 지연 비교가 데이터로 나온다. 하나만 만들면 증명 과제가 성립하지 않는다.
- **구체 설계**: [docs/ai-roadmap.md](docs/ai-roadmap.md)(조달 원칙·실행 순서·마일스톤 단일 출처) · [docs/ai-shape-synthesis.md](docs/ai-shape-synthesis.md)(1단계: 키워드→타일 도형) · [docs/ai-level-generation.md](docs/ai-level-generation.md)(2단계: 자연어→레벨)

### ADR-008 — 풋홀드(선분) 기반 충돌 모델
- **결정**: 지형 충돌을 "칸 단위 솔리드 타일 AABB"가 아니라 **풋홀드(foothold) = 바닥 선분**으로 한다. 타일맵은 **시각(코스메틱)** 전용으로 분리하고, 충돌은 `world::FootholdMap`이 담당한다.
- **이유**: 메이플스토리 손맛의 핵심(경사 보행, 원웨이 플랫폼, ↓+점프 드롭다운)이 선분 모델에서 자연스럽게 나온다. 칸 AABB로는 경사·원웨이가 부자연스럽다.
- **트레이드오프**: 시각(타일)과 충돌(풋홀드)을 **이중 관리** → 에디터가 둘 다 편집해야 함. 선분 질의 로직이 칸 인덱싱보다 복잡. 풋홀드 수 증가 시 선형 탐색 비용↑ → P-007 QuadTree(ADR-004)와 연계 필요.
- **트레이드오프 메모(ADR-004와의 관계)**: ADR-004의 AABB/QuadTree는 **몬스터·피격·아이템 등 박스형 동적 충돌**에 그대로 유효. 지형 착지만 풋홀드로 분리한 것 — 두 모델은 공존한다.
- **증명 과제**: 메모리 측면 — 맵을 픽셀이 아닌 **타일 인덱스 배열 + 풋홀드 선분 목록**으로 저장(낱개 PNG/픽셀 맵 대비 용량 비교). 스프라이트는 **아틀라스(텍스처 패킹)** 로 도입해 드로우콜·VRAM 오버헤드를 측정·기록(낱개 PNG 다수 방식 대비).

> **모듈 추가**: `world/`(TileMap·Foothold·FootholdMap·Map), `editor/`(MapEditor), `core/Camera`. 입력은 `platform/Input`으로 SDL과 디커플(ADR-006). 물리는 `MoveIntent` 추상 입력만 받아 platform과도 분리.

### ADR-009 — 에디터/런타임 실행파일 분리 (공통 엔진 정적 라이브러리)
- **결정**: 편집과 플레이를 **두 개의 실행파일**로 분리한다. `GuideStoryEngine`(정적 라이브러리)이 공통 코드(math/platform/world/physics/core/editor)를 담고, `GuideStoryEditor.exe`(편집·저장)와 `GuideStoryGame.exe`(맵 파일 로드·플레이)가 각자 `main` + 호스트 루프를 갖는다. 두 앱은 라이브러리를 통해 `SDLWindow`/`SDLRenderDevice`를 공유한다. 월드 렌더(`core::RenderWorld`)와 기본 맵 빌더(`world::BuildDefaultMap`), GUI 위젯(`core::Ui` — 메뉴/툴바)은 라이브러리로 추출해 두 앱이 공유한다(중복 제거). 에디터도 게임과 같은 화면(Screen) 골격을 갖는다: 선택 화면(런처)에서 맵/플레이어/스킬/몬스터/NPC 에디터로 분기하고, 각 에디터는 빈 화면에서 새로 만들기·열기로 시작한다.
- **이유**: 런타임 빌드에 편집 UI/툴 코드를 포함하지 않는다(관심사 분리·배포 표면 축소). 에디터는 추후 몬스터/스킬 배치 등 "변경 가능한 리소스" 편집의 home이 된다. 데이터(맵 파일)가 두 프로그램의 유일한 계약이 되어 데이터 주도(ADR-005)와 정렬된다.
- **트레이드오프**: 솔루션이 1→3 프로젝트로 늘고 빌드 설정(SDL include/lib, DLL 복사, vcpkg 매니페스트 경로 참조)이 앱마다 중복된다. ~~공통 호스트 루프 코드(타이밍·dt 클램프)가 두 앱에 약간 중복된다.~~ → **상환됨**: 타이밍·dt 클램프·폴·Present 골격을 `core::HostLoop`(Template Method)으로 추출, 두 앱은 `Frame()`만 구현한다. dt 클램프 상수가 단일 출처가 됨. (설계 패턴 적용 근거: [docs/design-patterns.md](docs/design-patterns.md) §2.4)
- **증명 과제**: 런타임 바이너리가 에디터 코드에 의존하지 않음을 보장. 데이터 파일만으로 에디터↔게임이 연결됨을 검증(에디터 저장 → 게임 로드).

### ADR-010 — AI는 제안하고, 사람이 확정한다
- **결정**: AI 생성 결과를 맵에 직접 쓰지 않는다. 항상 **고스트 레이어**로 표시하고 사람의 수락(Enter)이 있어야 타일이 된다. 수락·거절·수정 이력을 기록한다.
- **이유**: 검증되지 않은 모델이 자산을 직접 변경하면 되돌리기가 어렵고 실패작이 파일로 남는다. 그리고 **사람이 고친 최종본이 다음 학습 데이터가 되어야** 데이터 플라이휠이 성립한다 — 쓸수록 내 스타일에 가까워지는 루프가 이 설계의 자산이다.
- **트레이드오프**: 자동화 이득 일부 포기(키워드 한 번에 완성 파일이 나오지 않는다). 에디터에 고스트 레이어·후보 순환 UI가 추가로 필요.
- **증명 과제**: 피드백 로그로 재학습했을 때 인식률·다양성 지표가 개선되는지 **세대별로** 기록.

### ADR-011 — 생성 모델과 결정론적 후처리를 분리한다
- **결정**: 신경망은 **타일 격자**만 생성한다. 풋홀드 추출·경사 병합·스폰 배치·연결성 정리는 규칙 기반 후처리가 맡으며, 그 **정본은 C++ 에디터**에 둔다(수락 시점에 타일을 확정하는 자리에서 풋홀드도 만든다).
- **이유**: 풋홀드까지 학습시키려면 데이터가 수십 배 필요한데, 규칙으로 정확히 풀리는 문제다. 실패 지점도 분리된다 — 도형이 이상하면 모델 탓, 못 밟으면 후처리 탓. 프로토콜도 격자만 실으면 되어 계약이 얇아진다(ADR-008: 타일=코스메틱, 풋홀드=충돌).
- **트레이드오프**: 후처리 규칙이 도형 종류를 타면 규칙이 늘어난다. 모델이 "발판으로 쓰기 좋은 형태"를 학습할 기회를 포기한다. 파이썬 평가용 재구현과의 **이중 관리**(`gsmap.py`가 `Map.cpp`를 미러링하는 것과 같은 위험).
- **증명 과제**: 후처리 전후 플레이 가능성 지표 비교. C++/파이썬 두 구현의 출력 일치 테스트.

### ADR-012 — 라벨은 닫힌 집합에서 시작한다
- **결정**: 1단계는 6개 고정 라벨(사각형·원·삼각형·십자·별·하트) + 에디터의 동의어 문자열 테이블. **자유 텍스트 입력을 받지 않는다.**
- **이유**: 자유 텍스트를 받는 순간 "무엇을 맞혔는지" 채점할 수 없다. 닫힌 집합이어야 IoU·인식률 같은 지표가 정의된다. 미등록 키워드는 추측하지 않고 거절한다.
- **트레이드오프**: 표현력 제한. 라벨 추가마다 학습 데이터가 필요.
- **증명 과제**: 2단계 전환 시, 미학습 표현("동그라미", "둥근 언덕")의 매핑 성공률 측정.

### ADR-013 — 자연어는 구조화된 스펙을 거쳐서만 생성기에 닿는다
- **결정**: LLM은 자연어 → `MapSpec` JSON 변환만 담당한다. 생성 모델은 JSON만 받고 자연어를 보지 않는다. **스펙은 사용자가 위젯으로 수정 가능한 중간 산출물**로 노출된다(ADR-010의 확장 — 파싱 결과 자체가 제안이다).
- **이유**: 실패 지점 분리(파싱 오류 / 생성 품질 / 후처리 오류를 각각 잰다). LLM이 없거나 실패해도 위젯 경로로 기능이 완결된다. 자연어를 직접 조건으로 넣으면 **조건 준수도를 정의할 수 없다** — "얼음동굴"의 정답 맵은 존재하지 않으므로 채점 기준을 스펙으로 만들어야 한다.
- **트레이드오프**: 스키마에 없는 뉘앙스는 표현할 수 없다("음산한 느낌"). 스키마 확장마다 위젯·학습 조건·분석기 **3곳을 동기화**해야 한다.
- **증명 과제**: LLM 필드 정확도와 생성 조건 준수도를 **분리해서** 측정. 위젯 단독 경로가 LLM 없이 완결됨을 검증.

### ADR-014 — 학습 라벨을 손으로 붙이지 않고 맵에서 측정한다
- **결정**: `tools/analyze.py`가 `.gsmap`에서 레이아웃 속성(발판 밀도·수직성·개방도·간격)을 계산해 라벨로 쓴다. 스펙에 맞춰 맵을 그리는 것이 아니라, **그린 맵에서 스펙을 역산한다.**
- **이유**: 조건 조합(테마×밀도×수직성×개방도) 수만큼 맵을 그리는 것은 불가능하다. 이 방식이면 기존 맵과 앞으로 그릴 모든 맵이 자동으로 학습 데이터가 된다.
- **트레이드오프**: 분석기가 정의한 것만 조건이 될 수 있다. 라벨과 평가가 같은 함수라 **자기 채점 위험**이 있다 — 분석기를 속이는 법을 배울 수 있다.
- **증명 과제**: 분석기 라벨의 타당성(내가 의도한 밀도와 측정값의 상관). 자기 채점 완화 3종 — 홀드아웃 손그림, 사람 평가, **분석기와 독립적인** 플레이 가능성 시뮬레이션.

### ADR-015 — AI 부품은 하이브리드로 조달한다 (기성 모델 + 자체 학습)
- **결정**: 자연어 이해는 **기성 로컬 LLM**(Qwen2.5 Instruct GGUF + llama.cpp)을 가져다 쓰고, 타일·레벨 생성 모델은 **직접 설계·학습**한다. 판단 기준은 두 질문 — ①이미 잘 푸는 기성 모델이 있는가(있으면 가져다 쓴다) ②그 능력이 내 도메인 데이터에서만 나오는가(그렇다면 직접 만든다, ②가 ①을 이긴다). **기성 모델은 에디터에만 존재하고 게임 런타임 배포물에 포함하지 않는다.**
- **이유**: 자연어 이해를 처음부터 학습시키는 것은 GPU 클러스터와 수 주가 필요한데 재현해도 얻는 것이 없다. 반대로 "메이플풍 타일로 하트를 그리는 모델"은 대체할 기성 모델이 **아예 없고** 내 `.gsmap` 데이터에서만 나온다. 두 축을 갈라야 노력이 가치 있는 곳에 들어간다. 런타임 배제는 ADR-009("런타임 바이너리가 에디터 코드에 의존하지 않는다")의 연장 — 게임 실행에 필요한 건 `.gsmap` 하나뿐이라는 계약이 수 GB 모델로 깨지면 안 된다.
- **트레이드오프**: 기성 LLM 의존이 생긴다(모델 수 GB, 버전·라이선스 추적). "전부 직접 만들었다"는 단순한 서사를 포기한다. 대신 조달 판단의 근거가 산출물이 된다.
- **증명 과제**: 자체/기성의 경계를 **코드로** 검증 — 게임 빌드가 LLM 없이 완결됨, 에디터가 LLM 없이(위젯 경로로) 완결됨.

## 5. 증명 과제 대시보드 (지침 지표 추적)

> 이 프로젝트 "성공"의 척도. 각 항목은 코드 + **분석 문서/벤치마크**가 모두 있어야 완료.

| ID | 증명 과제 | 산출물 | 상태 |
|----|-----------|--------|------|
| ADR-001 | IOCP/Select 비교 + N명 동기화 검증 | 비교표, 검증 로그 | ◐ 진행 중 (1단계 계정+채팅 서버 구축·검증 완료. IOCP/epoll 비교와 월드 동기화 미착수) |
| ADR-002 | Custom Deleter RAII 래핑 기록 | 코드 + 회고 | ◐ 진행 중 (SDLWindow/SDLRenderDevice/SDL_Texture 적용, 회고 미작성) |
| ADR-003 | 상속→컴포넌트 전환 장단점 비교 | 비교 문서 | ☐ 미착수 |
| ADR-004 | 브루트포스 vs QuadTree 벤치마크 | ms/CPU 데이터, 한계 분석 | ☐ 미착수 |
| ADR-005 | 파싱 예외 처리 + 장단점 | 코드 + 회고 | ☐ 미착수 |
| ADR-006 | SDL 디커플링 결합도 분석 | 결합/디커플 비교 보고 | ◐ 진행 중 (Vector2D/Rect + IWindow/IRenderDevice 인터페이스 적용, 분석 미작성) |
| ADR-007 | C++↔Python 통신/추론 통합 | 지연 측정, 통합 기록 | ☐ 미착수 |
| ADR-008 | 풋홀드 충돌 + 인덱스 맵/아틀라스 메모리 비교 | 코드 + 용량/드로우콜 분석 | ◐ 진행 중 (풋홀드·인덱스맵·에디터 구현, 아틀라스·벤치 미작성) |
| ADR-009 | 에디터/런타임 실행파일 분리 + 공통 엔진 라이브러리 | 3-프로젝트 구조, 데이터 계약 검증 | ◐ 진행 중 (엔진 lib + 에디터/게임 exe 분리, 빌드·기동·맵 로드 검증 완료) |
| ADR-010 | 고스트 수락 UI + 피드백 재학습 효과 | 세대별 지표 추이 | ◐ 진행 중 (고스트/수락/재생성 UI 완료 — P-013. 피드백 재학습은 G2) |
| ADR-011 | 후처리 전후 플레이 가능성 + 두 구현 일치 | 지표 비교, 일치 테스트 | ◐ 진행 중 (`ai::ShapeToMap` 완료·23항목 검증. 파이썬 재구현 일치 테스트는 G2) |
| ADR-012 | 미학습 표현 매핑 성공률 | 리포트 | ☐ 미착수 (G5) |
| ADR-013 | LLM 파싱 정확도 / 생성 준수도 분리 측정 | 두 리포트 + 위젯 단독 경로 검증 | ☐ 미착수 (G4/G5) |
| ADR-014 | 분석기 라벨 타당성 + 자기 채점 완화 3종 | 상관 분석, 사람 평가, 독립 시뮬레이션 | ☐ 미착수 (G4) |
| ADR-015 | 자체/기성 경계의 코드 검증 | 게임 빌드·에디터가 LLM 없이 완결됨 | ◐ 진행 중 (AI 서버 없이 에디터 기동·편집 확인. LLM 경계는 G5) |

상태 표기: ☐ 미착수 · ◐ 진행 중 · ☑ 완료(코드+문서)

> ADR-010~015는 2.5단계(AI 서브시스템) 결정이다. 게이트(G0~G5)와 마일스톤(P-010~024)의 단일 출처는 [docs/ai-roadmap.md](docs/ai-roadmap.md)다.

## 6. 빌드 & 실행

**의존성**: SDL2는 vcpkg **매니페스트 모드**(`GuideStory/vcpkg.json`)로 관리한다.
빌드 전 의존성 복원이 필요하다(최초 1회, SDL2를 소스 빌드하므로 수 분 소요).

```powershell
# 0) VS 설치 경로 탐색 — 버전/에디션을 하드코딩하지 않는다.
#    (경로에 "2022"를 박아두면 VS를 올리는 순간 조용히 깨진다. 실제로 한 번 깨졌다.)
$vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
$vs      = & $vswhere -latest -property installationPath

# 1) SDL2 복원 (최초 1회) — VS 번들 vcpkg 사용
$vcpkg = "$vs\VC\vcpkg\vcpkg.exe"
$root  = "$PWD\GuideStory"            # vcpkg.json 위치
& $vcpkg install --triplet x64-windows --x-manifest-root=$root --x-install-root="$root\vcpkg_installed"

# 2) 빌드 (x64 전용 — Win32은 미지원, tech-debt D-006)
#    솔루션 전체를 빌드하면 엔진 라이브러리 → 에디터/게임 순으로 빌드된다.
$msbuild = "$vs\MSBuild\Current\Bin\MSBuild.exe"
& $msbuild GuideStory.sln /p:Configuration=Debug /p:Platform=x64

# 3) 실행
#    서버(계정/채팅): 게임보다 먼저 띄운다. 없어도 게임은 뜨지만 로그인이 안 된다.
.\x64\Debug\GuideStoryServer.exe            # 기본 7777 포트, guidestory.db
#    에디터: 선택 화면 → 맵 에디터에서 편집, [저장]/[열기]는 네이티브 파일 대화상자
.\x64\Debug\GuideStoryEditor.exe
#    게임: 로그인 → 메인화면 → 인게임(assets/maps 로드, 없으면 기본 맵으로 폴백)
.\x64\Debug\GuideStoryGame.exe
```

**서버만 따로 빌드**할 수도 있다. SDL을 쓰지 않으므로 vcpkg 복원이 필요 없다
(산출물은 솔루션이 아닌 `GuideStoryServer\x64\Debug\`에 놓인다).

```powershell
& $msbuild GuideStoryServer\GuideStoryServer.vcxproj /p:Configuration=Debug /p:Platform=x64
```

- **자산 폴더**: 에디터가 만든 맵 등은 리포 루트의 `assets/maps/`에 저장하고 게임이 같은 곳에서 로드한다.
  경로는 `platform::AssetsDir()`가 실행 파일에서 위로 올라가 `GuideStory.sln`을 찾아 결정한다(빌드 구성 무관).
  맨 파일명(포탈 대상·게임 기본 맵)은 `platform::MapPath()`가 이 폴더에 해석한다. (다음 단계: players/skills/monsters/npcs 하위 폴더.)
- **파일 대화상자**: 열기/저장은 Win32 공통 대화상자(`platform::OpenFileDialog`/`SaveFileDialog`)를 쓰며
  네이티브 의존은 `platform/FileDialog_Win32.cpp`에만 격리한다(ADR-006). `Comdlg32.lib`는 `#pragma comment(lib)`로 링크.
- SDL2 2.32.10 / SDL2_ttf 2.24.0 / SDL2_image 2.8.12 (x64-windows). 산출물 `vcpkg_installed/`는 `.gitignore` 제외(매니페스트로 재현).
- DLL은 PostBuildEvent가 출력 폴더로 자동 복사(Debug: `SDL2d.dll`, `SDL2_ttfd.dll`, `SDL2_imaged.dll`).
- **텍스처**: `IRenderDevice::LoadTexture(경로)`가 PNG를 로드·캐시(경로→핸들), `DrawTexture`로 그린다.
  `SDL_Texture`는 RAII 커스텀 deleter로 관리하고 렌더러보다 먼저 해제한다(ADR-002). 배경 이미지는 `assets/backgrounds/`.
- **배경/오브젝트**: 배경 PNG와 배치 오브젝트(건물 등)는 공유 `core::RenderWorld`가 **단일 출처**로 그린다 → 에디터와 게임이 동일하게 표시(게임이 배경을 따로 그리지 않는다).
  오브젝트는 1차로 단색 프리셋(`core::ObjectPalette` — 크기 타일 단위 + 색)이며, 추후 스프라이트로 확장. 충돌은 풋홀드가 담당(ADR-008).
- **배경이 카메라/시각 범위의 권위**: 배경이 설정된 맵은 `Map::WorldBounds()`가 배경 원본 픽셀 크기(맵 파일 `BGSIZE`)를 반환한다(없으면 타일 격자로 폴백). 타일 격자(32배수 양자화)가 배경보다 커도 배경 밖 빈 영역이 카메라에 노출되지 않는다. 배경 크기는 에디터가 렌더에서 측정해 맵에 심고 저장 시 기록한다. (데드존/스크롤박스 카메라는 추후 도입 — [docs/design-patterns.md](docs/design-patterns.md) 결정 절차에 따름.)
- **MapleStory 리소스**: WzComparerR로 추출한 배경/스프라이트는 WZ `.img` 노드 포맷이라 SDL_image로 직접 못 읽는다 — **PNG로 export** 후 `assets/`에 둔다(`.img/.wz` 직접 파싱은 별도 과제).
- **서버 접속 주소**: `assets/config/server.txt`(`HOST`/`PORT`)에서 읽는다. 빌드 없이 바꿀 수 있고,
  파일이 없으면 기본값 `127.0.0.1:7777`을 쓴다(서버를 안 띄우고 게임만 볼 때 오류가 아니다).
  키 설정·플레이어 상태와 같은 폴더, 같은 경량 텍스트 포맷이다.
- **WinSock 링크**는 `net/Socket.h`의 `#pragma comment(lib, "ws2_32.lib")`로 해결했다(D-005 상환).
  vcxproj마다 링크 설정을 중복하지 않고, 이 헤더를 쓰는 프로젝트가 자동으로 링크된다.
- **계정/채팅 DB**는 실행 폴더의 `guidestory.db`(WAL 파일 포함)에 생긴다 — `.gitignore` 제외.

## 7. 관련 문서

- 제품 정의: [PRODUCT.md](PRODUCT.md)
- 단계 로드맵: [PLANS.md](PLANS.md)
- 디자인 패턴 카탈로그·적용 지침: [docs/design-patterns.md](docs/design-patterns.md)
- AI 조달 원칙·실행 순서(마일스톤 단일 출처): [docs/ai-roadmap.md](docs/ai-roadmap.md)
- AI 1단계 — 키워드 → 타일 도형 생성: [docs/ai-shape-synthesis.md](docs/ai-shape-synthesis.md)
- AI 2단계 — 자연어 조건 레벨 생성: [docs/ai-level-generation.md](docs/ai-level-generation.md)
- AI 학습·추론 서브프로젝트(재현 절차): [GuideStoryAI/README.md](GuideStoryAI/README.md)
- 메카닉 구현: [docs/mechanics-harness.md](docs/mechanics-harness.md)
- 안정성·예외 정책: [RELIABILITY.md](RELIABILITY.md)
- 기술 부채: [tech-debt-tracker.md](tech-debt-tracker.md)
