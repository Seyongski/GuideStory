# PLANS.md — 개발 단계 로드맵 & 백로그

> 개발 프로세스는 4개 단계(1 → 2 → 2.5 → 3)로 진행한다.
> 각 단계는 [ARCHITECTURE.md](ARCHITECTURE.md)의 ADR 및 증명 과제와 연결된다.

## 1. 단계별 마일스톤

### 1단계 — 로컬 2D 플랫포머 (기반)
- [ ] 타일맵 배치 및 캐릭터 이동
- [ ] 발판 착지 및 점프 물리 구현
- [ ] (선행) `Vector2D`/`Rect` 자체 정의, `RenderDevice`/`Window` 인터페이스 — ADR-006
- [ ] (선행) `TextureManager`/`WindowManager` RAII + Custom Deleter — ADR-002
- 연결: ADR-002, ADR-006

### 2단계 — 데이터 및 콘텐츠 확장
- [ ] FSM 기반 몬스터 배치
- [ ] AABB 충돌 체크
- [ ] CSV/JSON 스킬·아이템 데이터 로드 시스템 (`DataManager` + 파싱 예외 처리)
- [ ] QuadTree 충돌 최적화 + 브루트포스 vs 쿼드트리 벤치마크
- [ ] (전환) 상속 → 컴포넌트 기반 게임 오브젝트
- 연결: ADR-003, ADR-004, ADR-005

### 2.5단계 — C++ 엔진과 AI 서브시스템 통합
> 목표: **키워드를 주면 AI가 타일로 도형을 그려내는** 에디터 기능. 나아가 자연어 조건("얼음동굴, 발판은 드문드문")으로 레벨을 구성한다.
> 게이트(G0~G5)·마일스톤(P-010~024)의 **단일 출처는 [docs/ai-roadmap.md](docs/ai-roadmap.md)** 다. 여기서는 게이트만 추적한다.

- [x] **G0 — 관통 완료**: 에디터 AI 패널 ↔ 소켓 ↔ Python 스텁 서버. 모델 없이 전 경로 검증 (P-010~013)
- [ ] **G1 — 데이터**: `.gsmap` 파이썬 파서, 합성 도형, 손그림 수집, 데이터셋 빌더 (P-014~015)
- [ ] **G2 — 첫 모델**: CVAE 학습·평가, 스텁을 실모델로 교체 (P-016)
- [ ] **G3 — 인프로세스**: TorchScript + libtorch, IPC vs 인프로세스 지연 비교 ← **ADR-007 완료 지점** (P-017)
- [ ] G4 — 조건부 레벨 생성: `MapSpec` + 에디터 위젯 + 분석기 + 조건부 모델 (P-018~022)
- [ ] G5 — 자연어: llama.cpp + GBNF 문법 제약 (P-023~024)
- 연결: ADR-007, ADR-010, ADR-011, ADR-012, ADR-013, ADR-014, ADR-015

> **순서 원칙**: 모델보다 **통합을 먼저** 뚫는다(G0). 파서·데이터·모델은 결과가 예측 가능한 작업이지만, C++↔Python이 에디터 UI까지 닿는 경로는 미지수다. G0의 파이썬 스텁은 절차적 래스터라이저이고, 이는 나중에 **비교 베이스라인 겸 합성 데이터 생성기**로 승격되므로 버려지지 않는다.
> **G3까지가 확정 목표**다. G4·G5는 불확실성이 커서 그때 가서 결정한다.

### 3단계 — 멀티플레이어 풀스택 전환
- [x] 클라이언트 코드를 서버/클라 구조로 분리 (`GuideStoryServer.exe` + `net/NetClient`)
- [x] WinSock 기반 패킷 구조체 정의 (`net/Protocol.h` — 서버·클라이언트 공유)
- [x] 계정(가입/로그인) + 채팅(전체/귓속말/시스템) — 서버가 신원의 단일 진실 원천
- [ ] 서버가 월드 상태(맵·좌표)를 갖고 이동을 판정 → 결과 브로드캐스트
- [ ] 멀티스레드 서버에서 여러 플레이어가 한 맵에서 동기화되는지 검증
- [ ] IOCP/epoll vs 접속당 스레드 비교 (현재 구현이 비교의 기준선)
- 연결: ADR-001

## 2. 현재 포커스

> **1단계 진행 중** — P-004 1차(풋홀드 충돌 + 인엔진 에디터 + 단색 렌더) 빌드/기동 검증 완료

- [x] AI 에이전트 하네스 문서 구조 생성
- [x] 실제 기획(2D 플랫포머 RPG, 클라/서버 분리, 증명 과제) 문서 반영
- [x] vcxproj에 C++20 표준 설정 (D-002 상환)
- [x] SDL2(vcpkg 매니페스트) 통합 및 빈 윈도우 띄우기 — 빌드/실행 검증 완료
- [x] `src/` 디렉터리·모듈 골격 생성 (core/math/platform/ecs/physics/ai/data/net)
- [x] P-004 1차: 입력(키/마우스)·렌더 프리미티브, world(TileMap+FootholdMap+텍스트 직렬화), 메이플식 PlatformerController(중력·점프·경사·원웨이·드롭다운), Camera, 인엔진 MapEditor (ADR-008)
- [x] 프로그램 분리: 단일 exe → **에디터/게임 2개 실행파일 + 공통 엔진 정적 라이브러리** (ADR-009). 빌드·기동·맵 로드(저장본/폴백) 검증 완료
- [x] P-009 1단계: **계정/채팅 서버**(`GuideStoryServer.exe`) + 클라이언트 `net::NetClient`(워커 스레드).
      로그인창이 실제 인증을 하고, 인게임 채팅(전체/귓속말)이 동작한다 (ADR-001)
- [x] AI 서브시스템 설계 확정 — 조달 원칙(ADR-015), 게이트 G0~G5, 마일스톤 P-010~024. 문서 3종 + `GuideStoryAI/` 명세
- [ ] **다음**: P-004 마감 — 스프라이트 아틀라스 도입(낱개 PNG 지양), 벽 막힘/사다리, 손맛 튜닝값 데이터화(ADR-005), 에디터 풋홀드 삭제·UI 텍스트
- [x] **P-010 완료(AI 착수)**: `GSMAP 3` — 모르는 태그 건너뛰기 + `CONCEPT` 라벨. `src/ai/` 인터페이스 골격
      (`AiTypes`/`IShapeGenerator`/`NullShapeGenerator`). **되돌릴 수 없는 결정(라벨 집합·요청/응답 계약)이 확정됐다.**
      포맷 회귀 12항목 통과(v2 하위호환 · 왕복 · 전방호환 · 필수누락 예외)
- [x] 파이썬 환경 구축·검증 — Python 3.12.10 + torch 2.13.0+cpu, venv `GuideStoryAI/.venv`, `requirements.txt` 고정.
      TorchScript 저장→재로드 포함 9항목 통과 (libtorch는 2.13.0으로 맞춘다)
- [x] **P-011 완료**: 절차적 래스터라이저(6개 도형) + 스텁 추론 서버 + 프로브. 와이어 규약 17항목 통과(재현성·분할/합침 프레임·거절 정책), 왕복 평균 0.43 ms
- [x] **P-012 완료**: `RemoteShapeGenerator`(워커 스레드 + 결과 큐) + 자체 JSON 파서. SDL2 복원 후 솔루션 전체 빌드 성공.
      C++↔파이썬 왕복 27항목 통과 — 재현성·요청 교체·서버 부재 타임아웃·프레임 규약. **Release 왕복 0.91ms / IPC 0.71ms**
- [x] **P-013 완료 → G0 관통**: 에디터 [AI 도형] 패널 — 드래그로 영역 선택 → 고스트 미리보기(예상 풋홀드 포함)
      → Enter 확정 / R 재생성 / Esc 취소. `ai::ShapeToMap`이 표면에서 풋홀드를 뽑고 스폰을 올린다(ADR-011).
      풋홀드 추출 23항목 + 엔드투엔드 11항목 통과. AI 서버 유무 양쪽에서 에디터 기동 확인
- [x] **P-014 완료**: `tools/gsmap.py` — C++ `Map.cpp`의 파이썬 미러. **바이트 동일 왕복**(CRLF + `%g` 서식 + 원문 보존).
      교차 언어 계약 양방향 검증 33항목. 검증 중 **`AIGEN`이 C++ 재저장에서 사라지는 것을 발견**해 보존 경로 추가
- [x] **P-015 (1/2) 완료**: 합성 데이터 10,000장(라벨당 2,000 × 5, **하트 홀드아웃**) + `tools/build_dataset.py`
      — 크롭·32x32 정규화·라벨별 증강 정책 강제(삼각형/하트 상하반전 0/300 확인) + 재현 정보(seed·해시·git) + PNG 몽타주.
      검증 28항목 통과
- [ ] **다음(AI, 사람 작업)**: P-015 (2/2) — 에디터로 **손그림 수집**. 라벨당 15장 이상, 하트는 손그림만.
      한 라벨 5장을 먼저 그려 파이프라인을 통과시킨 뒤 나머지를 그린다

## 3. 백로그

| ID | 항목 | 단계 | 관련 ADR | 상태 |
|----|------|------|----------|------|
| P-001 | C++20 표준 설정 | M0 | — | ✅ 완료 |
| P-002 | SDL2 링크 + 빈 윈도우 | 1 | ADR-006 | ✅ 완료 |
| P-003 | RAII Window/TextureManager | 1 | ADR-002 | 🔶 Window/RenderDevice 완료, TextureManager 남음 |
| P-004 | 타일맵 + 이동/점프 물리 | 1 | ADR-008 | 🔶 1차 완료(풋홀드+에디터+단색), 스프라이트·튜닝 데이터화 남음 |
| P-005 | AABB + FSM 몬스터 | 2 | ADR-003 | 예정 |
| P-006 | DataManager (JSON/CSV) | 2 | ADR-005 | 예정 |
| P-007 | QuadTree + 벤치마크 | 2 | ADR-004 | 예정 |
| P-008 | C++↔Python 통신 + libtorch | 2.5 | ADR-007 | 예정 |
| P-009 | WinSock 데디케이트 서버 | 3 | ADR-001 | 🔶 1단계 완료(계정+채팅, 프로토콜/프레이밍/세션), 월드 동기화·IOCP 비교 남음 |
| P-010 | `src/ai/` 골격(인터페이스+Null 폴백) + `GSMAP 3`(모르는 키 스킵 + `CONCEPT`) | 2.5 (G0) | ADR-011, ADR-015 | ✅ 완료 |
| P-011 | 절차적 도형 래스터라이저 + 스텁 추론 서버 | 2.5 (G0) | ADR-007 | ✅ 완료 |
| P-012 | `RemoteShapeGenerator` (`net/Framing` 재사용, 워커 스레드) | 2.5 (G0) | ADR-007 | ✅ 완료 |
| P-013 | 에디터 AI 패널(고스트/수락/재생성) + 풋홀드 추출 | 2.5 (G0) | ADR-010, ADR-011 | ✅ 완료 |
| P-014 | `tools/gsmap.py` (왕복 바이트 동일) | 2.5 (G1) | ADR-005 | ✅ 완료 |
| P-015 | 합성 데이터 + 손그림 수집 + 데이터셋 빌더(라벨별 증강) | 2.5 (G1) | ADR-012 | 🔶 합성 10,000장 + 빌더 완료, **손그림 수집 남음(사람 작업)** |
| P-016 | CVAE 학습 + 평가 리포트 + 스텁 교체 | 2.5 (G2) | ADR-010, ADR-012 | 예정 |
| P-017 | TorchScript export + `TorchShapeGenerator`(libtorch) + 지연 비교 | 2.5 (G3) | ADR-007 | 예정 |
| P-018 | 자기회귀 트랜스포머 v2 + 인페인팅 | 2.5 (G4) | ADR-007 | 예정 |
| P-019 | `MapSpec` 스키마 확정 (C++/Python 직렬화) | 2.5 (G4) | ADR-013 | 예정 |
| P-020 | 에디터 조건 위젯 (LLM 없이 완결) | 2.5 (G4) | ADR-013 | 예정 |
| P-021 | `tools/analyze.py` — 맵 → `MapSpec` 역산 | 2.5 (G4) | ADR-014 | 예정 |
| P-022 | 조건부 생성 모델 (스칼라 + 공간 마스크) | 2.5 (G4) | ADR-013, ADR-014 | 예정 |
| P-023 | LLM 레이어 (llama.cpp + GBNF 문법 제약) | 2.5 (G5) | ADR-013, ADR-015 | 예정 |
| P-024 | 텍스트 임베딩 조건화 / 확장 | 2.5 (G5) | ADR-012 | 예정 |

## 4. 완료 로그

| 날짜 | 항목 |
|------|------|
| 2026-06-10 | 하네스 문서 구조 생성 |
| 2026-06-10 | 실제 기획 내용 문서 반영 (PRODUCT/ARCHITECTURE/PLANS 등) |
| 2026-06-10 | C++20 설정 + SDL2(vcpkg) 통합 + src 골격 + 빈 윈도우 실행 검증 |
| 2026-06-10 | P-004 1차: 풋홀드 충돌 + 메이플식 이동/점프 물리 + 인엔진 맵에디터 (ADR-008), 빌드·기동 검증 |
| 2026-06-11 | 프로그램 분리: 에디터/게임 2개 실행파일 + 공통 엔진 정적 라이브러리 (ADR-009), 빌드·기동·맵 로드 검증 |
| 2026-08-25 | P-009 1단계: 계정/채팅 서버(GuideStoryServer.exe) + NetClient 워커 + 로그인/가입 화면 + 인게임 채팅 (ADR-001). 프로토콜 회귀 확인 20항목 통과, D-005 상환 |
| 2026-08-30 | AI 서브시스템 설계: ADR-010~015 추가, 게이트 G0~G5 + 마일스톤 P-010~024 확정. [docs/ai-roadmap.md](docs/ai-roadmap.md) · [docs/ai-shape-synthesis.md](docs/ai-shape-synthesis.md) · [docs/ai-level-generation.md](docs/ai-level-generation.md) · [GuideStoryAI/README.md](GuideStoryAI/README.md) |
| 2026-08-30 | P-010: `GSMAP 3`(모르는 태그 건너뛰기 + `CONCEPT`) + `src/ai/` 인터페이스 골격(`AiTypes`/`IShapeGenerator`/`NullShapeGenerator`). 포맷 회귀 12항목 통과 |
| 2026-08-30 | 파이썬 환경 구축·검증: Python 3.12.10(사용자 스코프) + venv + torch 2.13.0+cpu·numpy·matplotlib·tqdm 고정. TorchScript 저장→재로드 포함 9항목 통과. `.gitignore`에 `.venv`/`data`/`runs` 제외 |
| 2026-08-30 | P-011: 절차적 도형 래스터라이저(`tools/synth_shapes.py` — 스텁·베이스라인·합성 생성기 겸용) + 스텁 추론 서버(`serve/ai_server.py`) + 프로브(`serve/ai_client_probe.py`). 와이어 규약 17항목 통과, 왕복 평균 0.43 ms |
| 2026-08-30 | P-012: `ai::RemoteShapeGenerator`(워커 스레드 + 결과 큐, `net/Framing` 재사용) + `Json.h`(자체 최소 파서) + `AiProtocol.h`/`AiConfig.h`. SDL2 vcpkg 복원 후 솔루션 전체 빌드, C++↔파이썬 왕복 27항목 통과. Release 왕복 0.91ms(IPC 0.71ms) |
| 2026-08-30 | P-015(1/2): 합성 10,000장 생성 + `tools/build_dataset.py`(크롭·정규화·라벨별 증강 정책·재현 정보·몽타주). 검증 28항목 통과. `synth_shapes`의 격자 연산을 공개 API로 승격해 재사용 |
| 2026-08-30 | P-014: `tools/gsmap.py`(C++ `Map.cpp` 미러) — 바이트 동일 왕복 + 교차 언어 계약 양방향 검증 33항목. `world::Map`에 `AIGEN` 보존 추가(재저장 시 출처 유실 수정) |
| 2026-08-30 | P-013 → **G0 관통 완료**: 에디터 AI 패널(고스트/수락/재생성) + `ai::ShapeToMap` 풋홀드 추출·스폰 배치. 키워드 → 소켓 → 파이썬 → 격자 → 플레이 가능한 `.gsmap` 전 경로 검증(풋홀드 23항목 + E2E 11항목) |

## 5. 작성 규칙

- 각 항목은 **검증 가능한 완료 조건**을 가진다.
- 증명 과제가 걸린 항목은 **코드 + 분석 문서**가 모두 끝나야 완료.
- 완료 시 백로그 → 완료 로그로 이동하고 [ARCHITECTURE.md](ARCHITECTURE.md) 증명 대시보드 상태를 갱신한다.
