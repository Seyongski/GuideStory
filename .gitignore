# Visual Studio / MSBuild
.vs/
[Dd]ebug/
[Rr]elease/
x64/
x86/
*.user
*.aps
*.pdb
*.ilk
*.obj

# vcpkg manifest 설치 산출물 (vcpkg.json으로 재현 가능하므로 커밋 제외)
vcpkg_installed/

# 실행 캡처 로그(앱 stdout/stderr를 파일로 받은 것 — 커밋 대상 아님)
*.out
*.err
*_out.txt
*_err.txt
*_stderr.txt

# 서버 런타임 산출물 (계정/채팅 DB — 로컬 데이터, 커밋 대상 아님)
*.db
*.db-shm
*.db-wal
